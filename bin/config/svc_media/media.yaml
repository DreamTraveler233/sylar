media:
  # 推荐生产部署路径（可选）：/var/lib/im/media 或 /var/lib/im/uploads
  # 默认使用 /var/lib/im/media（如需要可改为绝对路径或相对 server.work_path）
  upload_base_dir: ../media
  temp_base_dir: ../media/tmp
  # 默认分片大小（字节）；可覆盖 MediaService 中的默认值
  shard_size: 2097152
